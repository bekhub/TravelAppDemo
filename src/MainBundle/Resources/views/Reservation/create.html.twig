{% extends 'MainBundle::layout.html.twig' %}

{% block title %}Забронировать тур{% endblock%}

{% block body %}

    <div class="container" style="width: 100%">
        <div class="row">
            <div class="col-md-4"></div>
            <div class="col-md-4">
                <div class="right-details-form padding-bottom">
                    <h3>{{ tour.title }} <br><br> Цена: ${{ tour.price }}</h3>
                    <h4>Забронировать тур</h4>
                    {% include 'MainBundle:Reservation:form.html.twig' with { 'form': form } %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}